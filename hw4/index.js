var car = document.getElementById("car");
var ctx = car.getContext("2d");

var score = document.getElementById("score")

var speed = 25;

var game = startGame(ctx, score, speed);

var game.init();

function startGame(ctx, score, speed) {
	
}